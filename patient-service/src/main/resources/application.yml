management:
    endpoints:
        web:
            exposure:
                include: refresh
spring:
    application:
        name: patient-service
    config:
        import: optional:configserver:http://localhost:8888
